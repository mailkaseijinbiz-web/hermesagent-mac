# Swift Package Manager build artifacts
.build/
release/

# macOS / Xcode system files
.DS_Store
.swiftpm/
Packages/
*.xcodeproj/

# Logs
logs
*.log

# Node (leftover from template) and local tooling
node_modules/
.claude/
.vscode/

# Secrets — never commit env/secret files (real secrets live in ~/.hermes/, outside the repo)
.env
*.env
secrets/
*.p8
*.p12
*.mobileprovision
